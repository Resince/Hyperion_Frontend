export enum liColor {
    PRIMARY = "primary",
    WARNING = "warning",
    SUCCESS = "success",
    DANGWR = "danger",
}

export enum liStatus {
    PENDING = "pending",
    SUCCESS = "success",
    FAILED = "failed",
}
